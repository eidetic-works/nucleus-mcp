# Envelope Codemod — Dry-Run Report

Generated by `scripts/codemod_envelope_tests.py`. Review candidates below before running with `--apply`.

**Total candidates:** 147

## `tests/test_align_ops.py`

- **Line 211** — parses dispatch result — candidate for unwrap()
  ```python
  data = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  data = _envelope.unwrap(json.loads(result))
  ```

## `tests/test_ambient_health.py`

- **Line 94** — direct dispatch call — check if result is later compared raw
  ```python
  """Test that dispatch() appends health footer."""
  ```

- **Line 99** — direct dispatch call — check if result is later compared raw
  ```python
  result = dispatch("ping", {}, router, "test_tool")
  ```

- **Line 106** — direct dispatch call — check if result is later compared raw
  ```python
  result = dispatch("fail", {}, router, "test_tool")
  ```

## `tests/test_artery_4_triggers.py`

- **Line 190** — direct dispatch call — check if result is later compared raw
  ```python
  def test_dedup_prevents_duplicate_dispatch(self, trigger_brain):
  ```

## `tests/test_artifact_feature_sync.py`

- **Line 59** — parses dispatch result — candidate for unwrap()
  ```python
  result = json.loads(result) if result.startswith("[") or result.startswith("{") else result
  ```
  Suggested rewrite:
  ```python
  result = _envelope.unwrap(json.loads(result)) if result.startswith("[") or result.startswith("{") else result
  ```

## `tests/test_cli_output.py`

- **Line 71** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

- **Line 93** — parses dispatch result — candidate for unwrap()
  ```python
  assert json.loads(result) == "hello"
  ```
  Suggested rewrite:
  ```python
  assert _envelope.unwrap(json.loads(result)) == "hello"
  ```

- **Line 167** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

## `tests/test_dispatch.py`

- **Line 171** — direct dispatch call — check if result is later compared raw
  ```python
  def _get_dispatch(self):
  ```

- **Line 178** — direct dispatch call — check if result is later compared raw
  ```python
  dispatch = self._get_dispatch()
  ```

- **Line 180** — direct dispatch call — check if result is later compared raw
  ```python
  result = json.loads(dispatch("", {}, router, "engrams"))
  ```

- **Line 185** — direct dispatch call — check if result is later compared raw
  ```python
  dispatch = self._get_dispatch()
  ```

- **Line 187** — direct dispatch call — check if result is later compared raw
  ```python
  result = json.loads(dispatch("nonexistent", {}, router, "engrams"))
  ```

- **Line 190** — direct dispatch call — check if result is later compared raw
  ```python
  def test_successful_dispatch(self):
  ```

- **Line 191** — direct dispatch call — check if result is later compared raw
  ```python
  dispatch = self._get_dispatch()
  ```

- **Line 193** — direct dispatch call — check if result is later compared raw
  ```python
  result = dispatch("greet", {"name": "world"}, router, "test")
  ```

- **Line 197** — direct dispatch call — check if result is later compared raw
  ```python
  dispatch = self._get_dispatch()
  ```

- **Line 199** — direct dispatch call — check if result is later compared raw
  ```python
  result = json.loads(dispatch("act", {"wrong_param": 1}, router, "test"))
  ```

- **Line 204** — direct dispatch call — check if result is later compared raw
  ```python
  dispatch = self._get_dispatch()
  ```

- **Line 208** — direct dispatch call — check if result is later compared raw
  ```python
  result = json.loads(dispatch("fail", {}, router, "test"))
  ```

- **Line 213** — direct dispatch call — check if result is later compared raw
  ```python
  dispatch = self._get_dispatch()
  ```

- **Line 215** — direct dispatch call — check if result is later compared raw
  ```python
  dispatch("act", {}, router, "test_mod")
  ```

- **Line 233** — direct dispatch call — check if result is later compared raw
  ```python
  async_dispatch("greet", {"name": "async"}, router, "test")
  ```

- **Line 233** — direct async_dispatch call — check consumers
  ```python
  async_dispatch("greet", {"name": "async"}, router, "test")
  ```

- **Line 247** — direct dispatch call — check if result is later compared raw
  ```python
  async_dispatch("act", {}, router, "test")
  ```

- **Line 247** — direct async_dispatch call — check consumers
  ```python
  async_dispatch("act", {}, router, "test")
  ```

- **Line 256** — direct dispatch call — check if result is later compared raw
  ```python
  async_dispatch("nope", {}, {"a": lambda: None}, "test")
  ```

- **Line 256** — direct async_dispatch call — check consumers
  ```python
  async_dispatch("nope", {}, {"a": lambda: None}, "test")
  ```

- **Line 271** — parses dispatch result — candidate for unwrap()
  ```python
  assert json.loads(result) == {"key": "val"}
  ```
  Suggested rewrite:
  ```python
  assert _envelope.unwrap(json.loads(result)) == {"key": "val"}
  ```

- **Line 276** — parses dispatch result — candidate for unwrap()
  ```python
  assert json.loads(result) == [1, 2, 3]
  ```
  Suggested rewrite:
  ```python
  assert _envelope.unwrap(json.loads(result)) == [1, 2, 3]
  ```

## `tests/test_dispatch_envelope.py`

- **Line 104** — direct dispatch call — check if result is later compared raw
  ```python
  result = _dispatch.dispatch("ok_dict", {}, ROUTER, "test_module")
  ```

- **Line 106** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

- **Line 114** — direct dispatch call — check if result is later compared raw
  ```python
  result = _dispatch.dispatch("nope", {}, ROUTER, "test_module")
  ```

- **Line 115** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

- **Line 123** — direct dispatch call — check if result is later compared raw
  ```python
  result = _dispatch.dispatch("raises_runtime", {}, ROUTER, "test_module")
  ```

- **Line 124** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

- **Line 135** — direct dispatch call — check if result is later compared raw
  ```python
  result = _dispatch.dispatch("ok_dict", {"k": "v"}, ROUTER, "test_module")
  ```

- **Line 136** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

- **Line 146** — direct dispatch call — check if result is later compared raw
  ```python
  result = _dispatch.dispatch("nope", {}, ROUTER, "test_module")
  ```

- **Line 147** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

- **Line 155** — direct dispatch call — check if result is later compared raw
  ```python
  result = _dispatch.dispatch("raises_runtime", {}, ROUTER, "test_module")
  ```

- **Line 156** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

- **Line 169** — direct dispatch call — check if result is later compared raw
  ```python
  result = _dispatch.dispatch("pre", {}, {"pre": _prewrap_handler}, "test_module")
  ```

- **Line 170** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

- **Line 182** — direct dispatch call — check if result is later compared raw
  ```python
  result = _dispatch.dispatch("s", {}, {"s": _str_handler}, "test_module")
  ```

- **Line 183** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

- **Line 189** — direct dispatch call — check if result is later compared raw
  ```python
  result = _dispatch.dispatch("", {}, ROUTER, "test_module")
  ```

- **Line 190** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

- **Line 203** — direct dispatch call — check if result is later compared raw
  ```python
  result = await _dispatch.async_dispatch("ok_async", {}, ROUTER, "test_module")
  ```

- **Line 203** — direct async_dispatch call — check consumers
  ```python
  result = await _dispatch.async_dispatch("ok_async", {}, ROUTER, "test_module")
  ```

- **Line 204** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

- **Line 211** — direct dispatch call — check if result is later compared raw
  ```python
  result = await _dispatch.async_dispatch("ok_async", {}, ROUTER, "test_module")
  ```

- **Line 211** — direct async_dispatch call — check consumers
  ```python
  result = await _dispatch.async_dispatch("ok_async", {}, ROUTER, "test_module")
  ```

- **Line 212** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

- **Line 220** — direct dispatch call — check if result is later compared raw
  ```python
  result = await _dispatch.async_dispatch("raises_async", {}, ROUTER, "test_module")
  ```

- **Line 220** — direct async_dispatch call — check consumers
  ```python
  result = await _dispatch.async_dispatch("raises_async", {}, ROUTER, "test_module")
  ```

- **Line 221** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

- **Line 235** — direct dispatch call — check if result is later compared raw
  ```python
  result = _dispatch.dispatch("ok_dict", {}, ROUTER, "test_module")
  ```

- **Line 236** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

## `tests/test_dispatch_telemetry.py`

- **Line 142** — direct dispatch call — check if result is later compared raw
  ```python
  result = dispatch("echo", {"msg": "test"}, SAMPLE_ROUTER, "nucleus_test")
  ```

- **Line 143** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

- **Line 152** — direct dispatch call — check if result is later compared raw
  ```python
  result = dispatch("error", {}, SAMPLE_ROUTER, "nucleus_test")
  ```

- **Line 153** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

- **Line 161** — direct dispatch call — check if result is later compared raw
  ```python
  dispatch("nonexistent", {}, SAMPLE_ROUTER, "nucleus_test")
  ```

- **Line 167** — direct dispatch call — check if result is later compared raw
  ```python
  dispatch("slow", {}, SAMPLE_ROUTER, "nucleus_test")
  ```

- **Line 174** — direct dispatch call — check if result is later compared raw
  ```python
  dispatch("echo", {"wrong_param": "x"}, SAMPLE_ROUTER, "nucleus_test")
  ```

- **Line 180** — direct dispatch call — check if result is later compared raw
  ```python
  dispatch("", {}, SAMPLE_ROUTER, "nucleus_test")
  ```

- **Line 192** — direct dispatch call — check if result is later compared raw
  ```python
  result = await async_dispatch("echo", {"msg": "hi"}, ASYNC_ROUTER, "nucleus_async")
  ```

- **Line 192** — direct async_dispatch call — check consumers
  ```python
  result = await async_dispatch("echo", {"msg": "hi"}, ASYNC_ROUTER, "nucleus_async")
  ```

- **Line 193** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

- **Line 201** — direct dispatch call — check if result is later compared raw
  ```python
  result = await async_dispatch("error", {}, ASYNC_ROUTER, "nucleus_async")
  ```

- **Line 201** — direct async_dispatch call — check consumers
  ```python
  result = await async_dispatch("error", {}, ASYNC_ROUTER, "nucleus_async")
  ```

- **Line 202** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

- **Line 209** — direct dispatch call — check if result is later compared raw
  ```python
  async def test_sync_handler_in_async_dispatch(self):
  ```

- **Line 209** — direct async_dispatch call — check consumers
  ```python
  async def test_sync_handler_in_async_dispatch(self):
  ```

- **Line 210** — direct dispatch call — check if result is later compared raw
  ```python
  result = await async_dispatch("sync_in_async", {}, ASYNC_ROUTER, "nucleus_async")
  ```

- **Line 210** — direct async_dispatch call — check consumers
  ```python
  result = await async_dispatch("sync_in_async", {}, ASYNC_ROUTER, "nucleus_async")
  ```

- **Line 211** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

- **Line 248** — direct dispatch call — check if result is later compared raw
  ```python
  dispatch("echo", {"msg": f"thread-{idx}"}, SAMPLE_ROUTER, "nucleus_test")
  ```

- **Line 274** — direct dispatch call — check if result is later compared raw
  ```python
  dispatch("echo", {}, SAMPLE_ROUTER, "nucleus_test")
  ```

## `tests/test_engram_tools.py`

- **Line 45** — parses dispatch result — candidate for unwrap()
  ```python
  result_data = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  result_data = _envelope.unwrap(json.loads(result))
  ```

- **Line 72** — parses dispatch result — candidate for unwrap()
  ```python
  result_data = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  result_data = _envelope.unwrap(json.loads(result))
  ```

- **Line 87** — parses dispatch result — candidate for unwrap()
  ```python
  result_data = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  result_data = _envelope.unwrap(json.loads(result))
  ```

- **Line 96** — parses dispatch result — candidate for unwrap()
  ```python
  result_data = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  result_data = _envelope.unwrap(json.loads(result))
  ```

- **Line 112** — parses dispatch result — candidate for unwrap()
  ```python
  result_data = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  result_data = _envelope.unwrap(json.loads(result))
  ```

- **Line 128** — parses dispatch result — candidate for unwrap()
  ```python
  result_data = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  result_data = _envelope.unwrap(json.loads(result))
  ```

- **Line 144** — parses dispatch result — candidate for unwrap()
  ```python
  result_data = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  result_data = _envelope.unwrap(json.loads(result))
  ```

- **Line 156** — parses dispatch result — candidate for unwrap()
  ```python
  result_data = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  result_data = _envelope.unwrap(json.loads(result))
  ```

- **Line 172** — parses dispatch result — candidate for unwrap()
  ```python
  result_data = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  result_data = _envelope.unwrap(json.loads(result))
  ```

- **Line 187** — parses dispatch result — candidate for unwrap()
  ```python
  result_data = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  result_data = _envelope.unwrap(json.loads(result))
  ```

- **Line 203** — parses dispatch result — candidate for unwrap()
  ```python
  result_data = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  result_data = _envelope.unwrap(json.loads(result))
  ```

- **Line 209** — parses dispatch result — candidate for unwrap()
  ```python
  result_data = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  result_data = _envelope.unwrap(json.loads(result))
  ```

- **Line 219** — parses dispatch result — candidate for unwrap()
  ```python
  result_data = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  result_data = _envelope.unwrap(json.loads(result))
  ```

- **Line 233** — parses dispatch result — candidate for unwrap()
  ```python
  result_data = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  result_data = _envelope.unwrap(json.loads(result))
  ```

## `tests/test_handler_return_types.py`

- **Line 5** — direct dispatch call — check if result is later compared raw
  ```python
  when called through dispatch(). This prevents the FastMCP
  ```

- **Line 36** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

- **Line 42** — parses dispatch result — candidate for unwrap()
  ```python
  assert json.loads(result) == [1, 2, 3]
  ```
  Suggested rewrite:
  ```python
  assert _envelope.unwrap(json.loads(result)) == [1, 2, 3]
  ```

- **Line 56** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

- **Line 65** — direct dispatch call — check if result is later compared raw
  ```python
  """Verify dispatch() always returns str regardless of handler return type."""
  ```

- **Line 69** — direct dispatch call — check if result is later compared raw
  ```python
  result = dispatch("ok", {}, router, "test")
  ```

- **Line 75** — direct dispatch call — check if result is later compared raw
  ```python
  result = dispatch("ok", {}, router, "test")
  ```

- **Line 77** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

- **Line 82** — direct dispatch call — check if result is later compared raw
  ```python
  result = dispatch("ok", {}, router, "test")
  ```

- **Line 84** — parses dispatch result — candidate for unwrap()
  ```python
  assert json.loads(result) == [1, 2, 3]
  ```
  Suggested rewrite:
  ```python
  assert _envelope.unwrap(json.loads(result)) == [1, 2, 3]
  ```

- **Line 88** — direct dispatch call — check if result is later compared raw
  ```python
  result = dispatch("ok", {}, router, "test")
  ```

- **Line 93** — direct dispatch call — check if result is later compared raw
  ```python
  result = dispatch("ok", {}, router, "test")
  ```

- **Line 100** — direct dispatch call — check if result is later compared raw
  ```python
  result = dispatch("ok", {}, router, "test")
  ```

- **Line 102** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

- **Line 107** — direct dispatch call — check if result is later compared raw
  ```python
  result = dispatch("missing", {}, router, "test")
  ```

- **Line 109** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

- **Line 114** — direct dispatch call — check if result is later compared raw
  ```python
  result = dispatch("", {}, router, "test")
  ```

- **Line 116** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

- **Line 170** — direct dispatch call — check if result is later compared raw
  ```python
  result = dispatch("check_recent", {}, router, "nucleus_sessions")
  ```

- **Line 172** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

- **Line 184** — direct dispatch call — check if result is later compared raw
  ```python
  result = dispatch("get_state", {}, router, "nucleus_sessions")
  ```

- **Line 194** — parses dispatch result — candidate for unwrap()
  ```python
  json.loads(result)  # Must be valid JSON
  ```
  Suggested rewrite:
  ```python
  _envelope.unwrap(json.loads(result))  # Must be valid JSON
  ```

- **Line 203** — parses dispatch result — candidate for unwrap()
  ```python
  json.loads(result)  # Must be valid JSON
  ```
  Suggested rewrite:
  ```python
  _envelope.unwrap(json.loads(result))  # Must be valid JSON
  ```

- **Line 240** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

- **Line 253** — parses dispatch result — candidate for unwrap()
  ```python
  parsed = json.loads(result)
  ```
  Suggested rewrite:
  ```python
  parsed = _envelope.unwrap(json.loads(result))
  ```

## `tests/test_health_sidecar.py`

- **Line 202** — parses dispatch result — candidate for unwrap()
  ```python
  assert json.loads(result)["ok"] is True
  ```
  Suggested rewrite:
  ```python
  assert _envelope.unwrap(json.loads(result))["ok"] is True
  ```

- **Line 216** — parses dispatch result — candidate for unwrap()
  ```python
  assert json.loads(result)["ok"] is True
  ```
  Suggested rewrite:
  ```python
  assert _envelope.unwrap(json.loads(result))["ok"] is True
  ```

- **Line 228** — parses dispatch result — candidate for unwrap()
  ```python
  assert json.loads(result)["error"]["error_type"] == "handler_error"
  ```
  Suggested rewrite:
  ```python
  assert _envelope.unwrap(json.loads(result))["error"]["error_type"] == "handler_error"
  ```

- **Line 240** — parses dispatch result — candidate for unwrap()
  ```python
  assert json.loads(result)["ok"] is False
  ```
  Suggested rewrite:
  ```python
  assert _envelope.unwrap(json.loads(result))["ok"] is False
  ```

## `tests/test_param_sanitization.py`

- **Line 144** — direct dispatch call — check if result is later compared raw
  ```python
  result = json.loads(dispatch("echo", None, ROUTER, "test"))
  ```

- **Line 148** — direct dispatch call — check if result is later compared raw
  ```python
  result = json.loads(dispatch("echo", {"ok": 1, "__proto__": "x"}, ROUTER, "test"))
  ```

- **Line 152** — direct dispatch call — check if result is later compared raw
  ```python
  result = json.loads(dispatch("echo", {"a": 1, "b": None}, ROUTER, "test"))
  ```

- **Line 156** — direct dispatch call — check if result is later compared raw
  ```python
  result = json.loads(dispatch("echo", "bad_params", ROUTER, "test"))
  ```

- **Line 164** — direct dispatch call — check if result is later compared raw
  ```python
  result = json.loads(dispatch("echo", d, ROUTER, "test"))
  ```

- **Line 169** — direct dispatch call — check if result is later compared raw
  ```python
  dispatch("echo", "bad", ROUTER, "test_mod")
  ```

## `tests/test_prometheus_dispatch.py`

- **Line 52** — direct dispatch call — check if result is later compared raw
  ```python
  dispatch("echo", {}, SAMPLE_ROUTER, "nucleus_test")
  ```

- **Line 53** — direct dispatch call — check if result is later compared raw
  ```python
  dispatch("echo", {"msg": "world"}, SAMPLE_ROUTER, "nucleus_test")
  ```

- **Line 61** — direct dispatch call — check if result is later compared raw
  ```python
  dispatch("error", {}, SAMPLE_ROUTER, "nucleus_test")
  ```

- **Line 68** — direct dispatch call — check if result is later compared raw
  ```python
  dispatch("echo", {}, SAMPLE_ROUTER, "nucleus_test")
  ```

- **Line 75** — direct dispatch call — check if result is later compared raw
  ```python
  dispatch("echo", {}, SAMPLE_ROUTER, "nucleus_tasks")
  ```

- **Line 76** — direct dispatch call — check if result is later compared raw
  ```python
  dispatch("echo", {}, SAMPLE_ROUTER, "nucleus_engrams")
  ```

- **Line 83** — direct dispatch call — check if result is later compared raw
  ```python
  dispatch("echo", {}, SAMPLE_ROUTER, "nucleus_test")
  ```

- **Line 98** — direct dispatch call — check if result is later compared raw
  ```python
  dispatch("echo", {}, SAMPLE_ROUTER, "nucleus_test")
  ```

## `tests/test_rate_limiting.py`

- **Line 12** — direct dispatch call — check if result is later compared raw
  ```python
  - Integration: rate-limited calls produce JSON error from dispatch()
  ```

- **Line 135** — direct dispatch call — check if result is later compared raw
  ```python
  r1 = json.loads(dispatch("echo", {}, ROUTER, "test_mod"))
  ```

- **Line 137** — direct dispatch call — check if result is later compared raw
  ```python
  r2 = json.loads(dispatch("echo", {}, ROUTER, "test_mod"))
  ```

- **Line 141** — direct dispatch call — check if result is later compared raw
  ```python
  r3 = json.loads(dispatch("echo", {}, ROUTER, "test_mod"))
  ```

- **Line 153** — direct dispatch call — check if result is later compared raw
  ```python
  dispatch("echo", {}, ROUTER, "telem_test")
  ```

- **Line 154** — direct dispatch call — check if result is later compared raw
  ```python
  dispatch("echo", {}, ROUTER, "telem_test")  # rate-limited
  ```

- **Line 166** — direct dispatch call — check if result is later compared raw
  ```python
  r1 = json.loads(dispatch("echo", {}, ROUTER, "mod_a"))
  ```

- **Line 168** — direct dispatch call — check if result is later compared raw
  ```python
  r2 = json.loads(dispatch("echo", {}, ROUTER, "mod_a"))
  ```

- **Line 172** — direct dispatch call — check if result is later compared raw
  ```python
  r3 = json.loads(dispatch("echo", {}, ROUTER, "mod_b"))
  ```
