startCommand:
  type: stdio
  configSchema:
    type: object
    properties:
      NUCLEUS_BRAIN_PATH:
        type: string
        default: ".brain"
        description: "Path to .brain/ directory for persistent storage"
    required: []
  commandFunction:
    |-
    (config) => ({
      command: "uvx",
      args: ["nucleus-mcp"],
      env: {
        NUCLEUS_BRAIN_PATH: config.NUCLEUS_BRAIN_PATH || ".brain"
      }
    })
